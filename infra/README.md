# AWS infrastructure

Reserved for the backend owner's AWS infrastructure and deployment configuration.

No cloud resources or infrastructure framework are configured here. Choose services, deployment tools, regions, permissions, budgets, and secret management during backend implementation. Preserve the project's zero-budget MVP goal; explicitly agree on any paid service before adopting it.

Frontend: `apps/web`. Python backend: `apps/api`. See `docs/backend-handoff.md` for the integration boundary.
