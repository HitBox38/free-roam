[project]
name = "free-roam-api"
version = "0.1.0"
description = "FreeRoam Python backend — implementation owned by the backend team"
readme = "README.md"
requires-python = ">=3.14.7,<3.15"
dependencies = []

[tool.uv]
package = false
